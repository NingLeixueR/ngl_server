REM File overview: Automates the rebuild workflow on Windows.

@echo off
REM Native CMake unity build is now used for project targets.
REM This legacy script is kept as a compatibility stub and no longer generates ub_*.cpp files.
echo Native CMake unity build is enabled. No ub_*.cpp files are generated anymore.
